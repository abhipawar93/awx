---
- name: Check Docker Version
  hosts: your_target_hosts
  become: yes  # Ensure privilege escalation if needed

  tasks:
    - name: Get Docker version
      docker_version_facts:
      
    - debug:
        var: ansible_facts.docker_version
